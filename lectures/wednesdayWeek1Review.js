let myArr = [10, 5, 8, 15];
// Loop through this array to display each of its values
for (let i = 0; i < myArr.length; i++) {
    console.log(myArr[i]); // Display the value of the array at the current index i
}

// Brief demo of a conditional (if) statement to run code only if a condition (or series of conditions) is met
let currentTemperature = 20;
if (currentTemperature < 30) {
    console.log("It's really cold!");
}